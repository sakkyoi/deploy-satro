---
title: Authoring Example of MDX
description: An example of authoring an MDX file.
pubDate: "2024-09-16"
heroImage: "example.png"
categories: ["example"]
tags: ["authoring", "mdx", "example"]
---

huan-gîng! fonˊngiangˇ! 你好! Hello! Willkommen! ようこそ! 🌏

This is an example of authoring an MDX file for your blog post.

# Heading

Headings are created by prefixing a line with one or more `#` characters. The number of `#` characters at the beginning of the line indicates the level of the heading.

> Note: table of contents is automatically generated based on the headings. You should start the heading level from `H1`, or the table of contents will not be generated. In addition, only `H1` and `H2` are displayed in the table of contents.

```mdx
# H1
## H2
### H3
#### H4
##### H5
###### H6
```

# H1

## H2

### H3

#### H4

##### H5

###### H6

# Texts

Text is written in paragraphs. Paragraphs are separated by one or more blank lines. You can use **bold**, *italic*, and ~~strikethrough~~ text. Also, you can use `inline code`.

And something special like:

<abbr title="Markdown for the component era">MDX</abbr>

H<sub>2</sub>O

(X - h)<sup>2</sup> / a<sup>2</sup> + (Y - k)<sup>2</sup> / b<sup>2</sup> = 1, or in LaTeX: $\frac{(X - h)^2}{a^2} + \frac{(Y - k)^2}{b^2} = 1$

Press <kbd>ALT</kbd> + <kbd>F4</kbd> to get something special.

<mark>highlight</mark> text

# Icons

```mdx
<iconify-icon icon="f7:cat" width="24"></iconify-icon>
```

You can use icons from [Iconify](https://icon-sets.iconify.design/) by using the `<iconify-icon>` component. <iconify-icon icon="f7:cat" width="24"></iconify-icon>

# Footnotes

Footnotes are created by wrapping the footnote text in square brackets `[]` and the footnote in parentheses `()`. The footnote will be displayed at the bottom of the page.

```mdx
This is an example of a footnote[^1].

[^1]: This is the footnote.
```

This is an example of a footnote[^1].

[^1]: This is the footnote.


# Links

Links are created by wrapping the link text in square brackets `[]` and the URL in parentheses `()`.

## External Links

```mdx
[Satro](https://github.com/sakkyoi/satro)
```

If the link is a reference to another site, it will open in a new tab. And will be have a icon next to it: [Satro](https://github.com/sakkyoi/satro).

## Internal Links

```mdx
[Readme](/article/readme)
```

If the link is a reference to another page on the site, it will open in the same tab: [Readme](/article/readme).

> Note: internal links must be prefixed with `/`.

## Anchor Links to Another Section

```mdx
[Jump to Heading](#heading)
```

If the link is a reference to another section on the same page, it will jump to that section: [Jump to Heading](#heading).

# Lists

Lists are created by prefixing lines with `-`, `*`, or `1.`.

## Unordered List

```mdx
- Unordered list item 1
- Unordered list item 2
    * Unordered list item 2.1
    * Unordered list item 2.2
    * Unordered list item 2.3
- Unordered list item 3
```

- Unordered list item 1
- Unordered list item 2
    * Unordered list item 2.1
    * Unordered list item 2.2
    * Unordered list item 2.3
- Unordered list item 3

## Ordered List

```mdx
1. Ordered list item 1
2. Ordered list item 2
3. Ordered list item 3
```

1. Ordered list item 1
2. Ordered list item 2
3. Ordered list item 3

# Images

Images are created by wrapping the image text in square brackets `[]`, the URL in parentheses `()` and start with an exclamation mark `!`.

```mdx
![Example](/example.png)
```

![Example](/example.png)

> Note: images must start with `/`. And the image file must be in the `image` directory. Subdirectories are allowed.

# Tables

Tables are created by using pipes `|` to separate columns and hyphens `-` to separate the header from the body.

```mdx
| Header 1 | Header 2 | Header 3 |
| -------- | -------- | -------- |
| Row 1    | Row 1    | Row 1    |
| Row 2    | Row 2    | Row 2    |
| Row 3    | Row 3    | Row 3    |
```

| Header 1 | Header 2 | Header 3 |
| -------- | -------- | -------- |
| Row 1    | Row 1    | Row 1    |
| Row 2    | Row 2    | Row 2    |
| Row 3    | Row 3    | Row 3    |

# Code Blocks

Code blocks are generated by [Expressive Code](https://expressive-code.com/). They are created by wrapping the code in triple backticks ` ``` `.

## With title

```javascript title="example-script.js"
$(window).on('keypress', (e) => {
    if (e.key == 'r') {
        alert('You pressed the "r" key!');
        const a = document.createElement('a');
        a.target = '_blank';
        a.href = 'https://www.youtube.com/watch?v=dQw4w9WgXcQ';
        a.click();
    }
});
```

## Line Marking

```javascript {7} del={2-3} ins={4,5} showLineNumbers=false
$(window).on('keypress', (e) => {
    if (e.key == 'r') {
        alert('You pressed the "r" key!');
    if (e.key == 'a') {
        console.log('You pressed the "a" key!');
        const a = document.createElement('a');
        a.target = '_blank';
        a.href = 'https://www.youtube.com/watch?v=dQw4w9WgXcQ';
        a.click();
    }
});
```

## Marking Individually

```javascript  startLineNumber=4 "_blank" ins=/wa.*=/ del=/a\..* /
        const a = document.createElement('a');
        a.target = '_blank';
        a.href = 'https://www.youtube.com/watch?v=dQw4w9WgXcQ';
        a.click();
    }
});
```

More information about code blocks can be found in the [Expressive Code documentation](https://expressive-code.com/key-features/syntax-highlighting/), there are so many features that you can use.
